name: secrets in run comment only
on: push
permissions:
  contents: read
jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: read
    steps:
      - run: |
          echo "safe"
          # ${{ secrets.TEST_TOKEN }} appears only in a comment
