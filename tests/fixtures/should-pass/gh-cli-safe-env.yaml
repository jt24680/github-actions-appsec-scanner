name: gh cli safe env usage
on: push
permissions:
  contents: read
jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: read
    steps:
      - run: gh issue create --title "$TITLE" --body "safe body"
        env:
          TITLE: safe-title
