name: combined-redirect-and-bracket-context
on:
  pull_request:
permissions:
  contents: read
jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
    steps:
      - run: |
          echo marker_GITHUB_ENV
          echo "x" > ${{ github['head_ref'] }}
