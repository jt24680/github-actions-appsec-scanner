name: credential-camouflage
on: push
permissions:
  contents: read
jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - run: echo deploying
        env:
          password: "actual-plaintext-secret${{ '' }}"
