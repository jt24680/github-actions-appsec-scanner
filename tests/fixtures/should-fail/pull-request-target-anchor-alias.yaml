name: PR target anchor alias trigger

x-events: &dangerous_events
  - pull_request_target

on: *dangerous_events

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: read
    steps:
      - run: echo "should fail exploit-guards"
