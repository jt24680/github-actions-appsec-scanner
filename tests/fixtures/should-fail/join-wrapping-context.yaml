name: join-wrapping-context
on:
  pull_request:
permissions:
  contents: read
jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - run: echo "${{ join(github.event.pull_request.labels.*.name, ',') }}"
