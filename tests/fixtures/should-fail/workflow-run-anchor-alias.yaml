name: workflow-run anchor alias trigger

x-events: &bridge_events
  - workflow_run

on: *bridge_events

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: read
    steps:
      - run: echo "should fail exploit-guards"
