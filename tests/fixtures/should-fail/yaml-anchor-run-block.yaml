name: yaml-anchor-run-block
on:
  pull_request:
permissions:
  contents: read
jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - run: &cmd |
          echo "${{ github.event.pull_request.title }}"
