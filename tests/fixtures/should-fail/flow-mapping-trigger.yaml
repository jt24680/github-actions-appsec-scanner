name: flow-mapping-trigger
on:
  pull_request_target: { types: [opened] }
permissions:
  contents: read
jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
    steps:
      - run: echo test
