name: untrusted-container
on:
  issues:
    types: [opened]
permissions:
  contents: read
jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
    container:
      image: ${{ github.event.issue.body }}
    steps:
      - run: echo test
